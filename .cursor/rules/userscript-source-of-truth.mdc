---
description: Tampermonkey userscript source-of-truth rule
alwaysApply: true
---

# Tampermonkey Userscript 修改规则

本项目的油猴脚本采用「源码拆分 + 构建生成单文件」的结构。详见仓库根目录 `AGENTS.md`。

## 1. 源码唯一目录

油猴脚本源码唯一目录：

`chatgpt-toolbox/tampermonkey-userscript-src/`

## 2. 构建脚本

`chatgpt-toolbox/build.userjs.mjs`

## 3. 生成文件（禁止直接修改）

- `chatgpt-toolbox/dist/client.user.js`
- `client.user.js`（仓库根目录）

这两个文件是构建产物，不是源码。看到生成文件里的问题时，必须回到源码目录查找对应模块后再修改。

## 4. 允许修改的路径

- `chatgpt-toolbox/tampermonkey-userscript-src/**/*.js`
- `chatgpt-toolbox/build.userjs.mjs`
- `chatgpt-toolbox/package.json`
- `chatgpt-toolbox/README.md`
- `.cursor/rules/**/*.mdc`
- `AGENTS.md`

## 5. 修改流程

每次修改油猴功能时，必须遵守：

1. 先定位 `tampermonkey-userscript-src/` 下的源码文件。
2. 只改源码与构建脚本，**不要**手动编辑 `dist/client.user.js` 或根目录 `client.user.js`。
3. 修改完成后在 `chatgpt-toolbox` 目录执行：

```bash
npm run build
```

4. 确认 `dist/client.user.js` 已重新生成（顶部应有 `GENERATED FILE - DO NOT EDIT DIRECTLY`）。
5. 确认根目录 `client.user.js` 已由构建脚本同步。

## 6. 异常处理

- 不要使用 `try/pass`（空 except 吞掉错误）。
- 如果必须捕获异常，必须 `console.error` 输出完整错误对象，不能静默吞掉异常。

## 7. 上传 UI 约定

- 项目分组 chip 栏（`#cgpt-upload-group-list` / `.cgpt-upload-groups-head`）必须始终在「开始上传」按钮上方显示。
- 精简模式、空项目时也不得隐藏分组栏；空列表仅在 `#cgpt-upload-list` 内显示「当前项目没有文件」。
- 修改 `UploadModule.mount` 的 DOM 后，须保持 `ensureUploadGroupSection` 与 `runUploadModuleInitPipeline` 中的防御逻辑可用。
