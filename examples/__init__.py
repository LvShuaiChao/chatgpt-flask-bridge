"""
@deprecated Legacy examples for manual debugging only.

This package is intentionally outside the main GUI runtime chain
(`gui.py` / `app.ui.main_window`). Keep these tools for source checkouts and
developer diagnostics, but exclude `examples/` from production release bundles.
"""
