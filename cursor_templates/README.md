# Cursor 找图模板

将以下 PNG 放在本目录（与小脚本 `images/` 目录中同名文件一致）：

- `upgrade to Pro.png`
- `编辑框的输入框位置.png`
- `输入框的agent按钮.png`
- `麦克风_editbox_bottom.png`

缺失时 GUI 仍可启动，会在 Cursor代码 页日志中提示 `[CURSOR_CODE][TEMPLATE_LOAD]` 错误。
